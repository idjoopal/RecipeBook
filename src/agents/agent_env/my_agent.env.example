# My Agent 환경변수 예시
# 실제 값은 my_agent.env에 설정 (gitignore됨)

# 사용할 LLM 모델 (llm_manager 라우팅 규칙 적용)
# 예: openai / openai:gpt-4o-mini (실제 OpenAI)
#     cohere:north-large / cohere:ziphyun (내부 Cohere 엔드포인트, .env의 COHERE_ENDPOINT_* 별칭)
MY_AGENT_MODEL=openai

# 외부 API 키 (필요 시)
MY_AGENT_API_KEY=
